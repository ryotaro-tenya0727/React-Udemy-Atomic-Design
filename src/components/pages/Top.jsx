import styled from "styled-components";

const SContainer = styled.div`
  text-align: center;
`;

export const Top = () => {
  return (
    <SContainer>
      <h2>TOPページです</h2>
    </SContainer>
  );
};
